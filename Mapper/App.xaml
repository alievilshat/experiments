<Application x:Class="ScriptModule.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:uiUtils="clr-namespace:ScriptModule.UIUtils"
             StartupUri="ScriptModule.xaml">
    <Application.Resources>
        <uiUtils:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <uiUtils:BoolToVisibilityConverter x:Key="InvertBoolToVisibility" Invert="true"/>
        <Style x:Key="PropertyName">
            <Setter Property="FrameworkElement.VerticalAlignment" Value="Center" />
            <Setter Property="FrameworkElement.HorizontalAlignment" Value="Right" />
        </Style>
        <Style x:Key="PropertyValue">
            <Setter Property="FrameworkElement.VerticalAlignment" Value="Center" />
            <Setter Property="FrameworkElement.HorizontalAlignment" Value="Left" />
        </Style>
    </Application.Resources>
</Application>
